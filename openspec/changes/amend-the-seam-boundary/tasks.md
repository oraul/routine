## 1. The seam is named by location, not by language

- [ ] 1.1 Red→green: `openspec/project.md`'s Law 5 states that `bin/`
      and `lib/` are core and destined for the binary, that the seam is
      exactly the app hooks and caffeine sidecars, and that a core
      script written in bash today says nothing about which side of the
      seam it is on; the pins in `test/guidance_content.bats` fail on
      the untouched document and pass on the amended one, and the
      existing seam and destination pins keep passing —
      `test/guidance_content.bats`
