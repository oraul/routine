## 1. The README hands over a model

- [ ] 1.1 Red→green: `README.md` opens by naming routine a working model
      of a spec-first loop rather than a plugin to install, carries a
      concept pipeline whose columns are phase, decider and what stops
      the run, names what transfers and what is this repository's
      accident, and demotes the install below the model — pinned in
      `test/guidance_content.bats`
