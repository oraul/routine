## 1. The developer reads its slice's briefing

- [x] 1.1 Red→green: `agents/developer.md` admits its own task's
      `briefing.md` to the closed context list and ranks its conventions
      with the target's own; `agents/analyst.md` requires the briefing to
      carry the conventions in force for its slice — pinned in
      `test/agents_content.bats`
