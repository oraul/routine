## 1. The page is computed, never curated

- [x] 1.1 Red→green: `routine-panel` prints a self-contained HTML page
      from script-owned state, writing nothing, with every signal
      derived and no external asset

## 2. The signals carry the run

- [x] 2.1 Red→green: status, latency, traffic, errors, saturation and
      the caffeine queue each render from real state, and an empty
      corpus renders an honest empty page
