## 1. The checker

- [ ] 1.1 Red→green: sensitive-pattern scan over diff (with self-exclusion)
      and commit messages, each hit named
- [ ] 1.2 Red→green: commit grammar — conventional subject, 72-char cap,
      Change trailer on behavior types, merge commits exempt

## 2. CI

- [ ] 2.1 Add the `conventions` job running the check against the PR base
