## 1. The prompts consult, never recall

- [ ] 1.1 Red→green: the skill and both agents name the frontmatter and
      `routine-manual` as the authoritative script contract — pinned in
      `test/agents_content.bats`
