## 1. The exemption narrows to what it can prove

- [ ] 1.1 Red→green: a negation on `$output` is exempt from subject
      pairing only when the body also asserts `$status`; the one
      violating test — `script failures name scripts, never scenario
      labels` — gains its status assertion, and the corpus stays green
