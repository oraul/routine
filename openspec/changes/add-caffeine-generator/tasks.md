## 1. Discovery script

- [x] 1.1 Red→green: `bin/routine-deps` extracts `ruby/<gem>` topics from a
      Gemfile fixture
- [x] 1.2 Red→green: package.json (dependencies + devDependencies) and
      requirements.txt extraction; no-manifest exits non-zero naming the
      three manifests; multiple manifests combine

## 2. Generation skill

- [ ] 2.1 Write `skills/caffeinate/SKILL.md`: discovery via routine-deps,
      human topic selection, the sidecar contract (3–5 grep-able rules, one
      fixture per rule, .md for judgment), selfcheck-green completion
