## 1. The cost becomes a number

- [ ] 1.1 Red→green: the retro's re-specify cost section — per
      app+ticket episodes, gate-semantics failed-lint counts, and
      defect-to-recovery seconds, unrecovered named
