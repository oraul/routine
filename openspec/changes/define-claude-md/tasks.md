## 1. The contract

- [ ] 1.1 Write `CLAUDE.md` to the guidance spec: spec-first rule, hard
      rules, laws and conventions pointers, the three commands, TDD rule,
      script-owned-state prohibition — one screen, pointers over prose
