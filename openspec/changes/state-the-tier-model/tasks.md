## 1. The session contract states the tier model

- [ ] 1.1 Red→green: `CLAUDE.md` states the three tiers, the rule that
      picks them, the never-delegate-the-record boundary, and the limit
      that routine checks declaration rather than effect — pinned by a
      new `test/guidance_content.bats`, whose pins for already-required
      contract content are characterization, not TDD evidence

## 2. The public face admits the third agent

- [ ] 2.1 Red→green: `README.md` describes the loop's two phase-driving
      agents and the read-only scout as distinct, pinned in the same file
