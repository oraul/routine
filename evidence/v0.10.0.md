# Release record: v0.10.0

Compared against v0.9.0. The range is the three merges in `v0.9.0..HEAD`; the citation lint holds every `#N` to it. Ticket 0006 concluded inside this range and supplies the run evidence.

## Caffeine

- **The test lint pays per suite, not per test.** The cost was launch overhead, not work, and the fix was measured on the same instruments before and after.

  evidence: 6,902 execve and 13.1s over 446 tests became 62 execve and 0.17s (#94); the fixture shim tally fell 608 → 2; selfcheck fell from its 95s trend to ~56s on main; the macOS CI test job from ~2.5 minutes to 78 seconds. The pin is a PATH-shim launch counter bounded per suite file — strace is absent on the macOS runner — red at 608 against a bound of 30, green at 2, the bound encoding the scaling law rather than the implementation count. Behaviour was frozen and proven: old and new verdicts byte-identical on stdout, stderr and exit over an adversarial corpus exercising every rule family.

  lives in: `bin/routine-test-lint`, `openspec/specs/selfcheck/spec.md`

- **A claim is measured or a prediction, never a third thing.** The operator's rule, landed as contract where claims travel ungraded.

  evidence: `CLAUDE.md`'s rails and `agents/analyst.md`'s Rules both carry it, test-pinned on words verified absent at HEAD (#95). The archived proposal holds the four exhibits: a published count wrong by three, ten relays that held by luck, a structural claim built from a timing number, and the counter-example — #94's forecast, labelled with its instruments named, settled at 0.17s and 62 launches with nothing to walk back.

  topic: testing/tdd

- **`routine-tdd characterize` ran live, in both directions.** The oldest open Gate entry — zero executions, capability asserted rather than observed — is closed by run 0006.

  evidence: seven `tdd.characterize` events in the archived ticket, four passes proving birth claims and three refusals writing the first real `characterize.log`s. One refusal caught a claim that was not yet true; two caught commands that never ran — the exact nothing-ran-is-not-evidence case the phase was built for.

  lives in: `runs/shopapp/tickets/archive/0006/telemetry.jsonl`, `bin/routine-tdd`

- **The whole v0.9.0 release fired in one ticket.** Its pieces were built across three changes and had never run together.

  evidence: the cross-contract fix let run 0006 start; the `## Questions` section parked it at approve — `routine-approve` exit 1, no `ticket.approve` line — until the operator's note ruled on all four questions; the rulings were implemented exactly (customer-favour rounding, Integer-only 1..100, compounding list, no deeper seal, `confirm!` untouched); four tasks concluded through the audit. The run also carried one honest refused `tdd.green` — a developer had written the next scenario's test early — recorded, explained, and accepted by the audit as a refusal rather than a hole.

  lives in: `runs/shopapp/tickets/archive/0006/`

- **The operator-question loop closed end to end.** Raised by the analyst, shown at approve, ruled in a note, implemented verbatim — the first full round trip of intent through the interface built for it.

  evidence: the approve note in the archived ticket records all four rulings; task 01-02's red proved 2044 wrong and 2043 right, task 01-03's third red proved `12.5.between?(1,100)` still passed until the Integer check landed, task 01-04's scenarios assert the guard's own message rather than the seal's.

  lives in: `runs/shopapp/tickets/archive/0006/approve.md`, `/home/user/shopapp` at `b4a5753`

## Gate

- **A passing characterize left the refusal's stale log behind, three times, and the fix landed inside this same range.** The interface built to deliver verbatim evidence would have delivered wrong evidence confidently.

  evidence: three done tasks in the archived ticket hold a `characterize.log` from long-resolved slips — two wrong-cwd LoadErrors, one `cd` error (#96 carries the file list). The analyst's re-entry rule reads that file as the current failure and `routine-defect` attaches it. Fixed in #96: a pass removes the log; the refusal history stays in telemetry. The archived logs stay as the instances that earned the fix — script-owned history, inert because `routine-defect` cannot fire on an archived ticket.

  script that would decide it: decided — the #96 test refuses once, passes once, asserts the log gone.

- **Delegate self-reports omitted refused attempts, two runs out of three.** The rails caught every omission; nothing scripted depends on a report being complete.

  evidence: developers 01-01 and 01-03 omitted their refused `characterize` attempts; telemetry showed both as exit-1 events before the pass. Developer 01-04, whose payload said the record would be compared, reported both of its refusals unprompted, including one under a different command hash. Two omissions is a pattern by this repository's standard.

  script that would decide it: none can — a report is transcript, not artifact, so no script sees it. The control is the driver comparing report to telemetry, which went three for three; the candidate contract line rides with the queued developer-contract work rather than earning its own change.

- **The rails-versus-easier-requirements question is still open, run seven included.** Ticket 0006's near-clean finish spans changed rails and a changed requirement at once.

  evidence: run 0006 concluded with one honest refusal and zero improvisations, but its requirement (a discount feature) differs from every predecessor's, so nothing here attributes the improvement. The archived requirements and anchors make the replay buildable — six requirements, held verbatim, against the current rails with the target reset per anchor.

  script that would decide it: the replay harness, still unbuilt, still the only instrument that separates the two explanations.

- **Ticket 0001's false completion still stands unexamined.** Reported done, shipped nothing, and no gate has been taught to notice since v0.9.0 named it.

  evidence: unchanged — no discount code exists at any commit before `b4a5753`, yet ticket 0001 ("Order discounts") concluded. Run 0006 implemented what 0001 claimed to, which retires the missing feature and leaves the missing gate.

  script that would decide it: a target-side mutation check — does reverting a ticket's work redden that ticket's own tests — and nothing runs one against a target today.

- **Two proving-run commits in the target are authored experimental conditions.** Standing disclosure, one release on.

  evidence: `ad24ee0` and `430ca25` were written to falsify specific claims at specific moments, with messages styled as a colleague's. Run 0006's guard order and message assertions lean on `430ca25` directly, so the disclosure stays load-bearing.

  script that would decide it: none, and none wanted — a target that never surprises the loop proves less.
